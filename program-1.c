#include <stdio.h>

int main() {
    int celsius = 38;
    int fahrenheit;

    // Integer calculation
    fahrenheit = (9 * celsius) / 5 + 32;

    printf("Temperature in Fahrenheit: %d\n", fahrenheit);

    return 0;
}